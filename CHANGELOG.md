# 更新記錄

> 這份記錄是內部開發紀錄的公開整理版，移除了測試時使用的真實專案名稱與金額，只保留
> 「發生什麼問題／怎麼修的／怎麼驗證的」這個層次的說明。

## 施工日報列印版面調整

- 表頭新增「超前/落後(%)」欄位（實際進度 − 預定進度，直接參照已算好的儲存格，避免重複計算）。
- 工地安全檢查三項欄位（實施勤前教育／確認新進勞工提報勞保／檢查勞工個人防護具）改成
  「□有 □無」勾選樣式的文字顯示（依日資料庫既有的下拉選單值動態產生）。
- 欄寬依A4紙張可用寬度等比例縮放，列印邊界維持既有設定。
- 系統擴充區塊（施工進度統計彙總表）預設摺疊隱藏且不列印，改用動態變數計算隱藏範圍
  （不寫死列號），確保不同案子（不同工項/中項數量）都能正確運作；「一、施工項目表」欄名列
  設為每頁重複列印表頭。
- 移除「一、施工項目表」的備註欄，釋出的欄寬併給「累計完成數量」欄。

## 支援「變更設計比較表」格式

- 新增專屬解析器，支援機關/廠商常見的「變更設計比較表」（同一列並列原契約/變更後兩組
  數量單價、多一層阿拉伯數字群組編號），跟標準 PCCES「詳細價目表」格式並存，自動偵測套用。
- 支援「議價明細表」：契約變更新增的工項如果有議價後正式單價，優先採用。
- 新增工項名稱重複偵測警告：若契約清單裡有兩個以上工項使用完全相同的名稱，逐日資料比對
  可能誤植，程式現在會明確列出警告，提醒人工核對。

## 契約變更快速套用 + 工期展延

- 新增「套用契約變更」功能：把變更後的新工項清單套用到已經填了逐日資料的舊活頁簿上，
  保留已填資料，找不到對應的舊工項會整理進「已刪除工項歷史記錄」分頁，不會憑空消失。
- 新增工期展延輸入：填「展延後完工日期」即可自動反推工期天數，不需自己心算天數。
- 修正上傳 `.xls` 檔案時因暫存檔名遺失副檔名導致誤判「不支援的副檔名」的 GUI bug。

## 中項摺疊 + 工程進度儀表板

- 「日資料庫」「施工日報」「儀表板」的中項欄位/列改用 Excel 原生大綱分組功能預設摺疊，
  資料還在，只是先收合，列印時也會自動跳過摺疊範圍。
- 新增「儀表板」分頁：KPI 總覽（契約總金額/預定進度/實際進度/落後百分點等）、大項/中項/
  工項進度總覽表格，依落後程度自動標色。已知限制：目前的「落後」判斷是拿全案整體一條 S
  曲線當比較基準，不是每個工項各自獨立的排程，需人工複核。

## 免安裝拖拉式 GUI 工具

- 用 Pyodide（瀏覽器內執行 CPython／WebAssembly）技術，把既有 CLI 工具包成單一 HTML 檔案，
  不需安裝 Python，拖拉檔案即可使用，所有運算都在瀏覽器本機完成，不上傳任何資料到伺服器。

## 契約總價判斷邏輯修正

- 修正契約總價判斷過於嚴格導致誤判、把「賸餘價值折價費」等契約金額外的扣款項目也算進總價
  的問題；改成優先信任文件本身標示的總價/總計列，找不到才自行加總，且一律印出量化過的
  差異提醒，不靜默算錯或算對但不吭聲。
- 新增「PCCES來源核對」除錯分頁，列出每一列原始解析結果，方便使用者自行核對。

## 通用化 CLI 工具

- 把原本針對單一案子寫死的工具改寫成不含任何專案特定假設（工項數／大項邊界／欄位位置）的
  通用化命令列工具，換一個新案子直接餵新的 PCCES 檔案即可使用。
